openapi: 3.1.0
info:
  title: Hidden Grain API
  version: 1.1.0
  description: |
    Canonical wire contract for Hidden Grain. As of EPIC-006.0 the API also
    exposes the Perception Experiment domain (experiments, stimuli, sessions,
    responses). Knowledge Object endpoints remain from prior EPICs.
servers:
  - url: /api
paths:
  /health:
    get:
      summary: Service health
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthResponse" }
  /objects:
    get:
      summary: List knowledge objects
      parameters:
        - { in: query, name: q,        schema: { type: string } }
        - { in: query, name: type,     schema: { type: string } }
        - { in: query, name: category, schema: { type: string } }
        - { in: query, name: status,   schema: { type: string } }
        - { in: query, name: tag,      schema: { type: string } }
        - { in: query, name: offset,   schema: { type: integer, minimum: 0 } }
        - { in: query, name: limit,    schema: { type: integer, minimum: 1, maximum: 100 } }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ObjectsListResponse" }
    post:
      summary: Create a knowledge object
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateKnowledgeObjectRequest" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ObjectDetailResponse" }
        "422": { description: Validation failed }
  /objects/{id}:
    get:
      summary: Object detail
      parameters:
        - { in: path, name: id, required: true, schema: { type: string } }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ObjectDetailResponse" }
        "404": { description: Not found }
  /graph:
    get:
      summary: Full knowledge graph
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GraphResponse" }
  /relationships:
    post:
      summary: Create a relationship
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateRelationshipRequest" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GraphEdge" }
        "422": { description: Validation failed }
  /experiments:
    get:  { summary: List perception experiments,   responses: { "200": { description: OK } } }
    post: { summary: Create a perception experiment, responses: { "201": { description: Created }, "422": { description: Validation failed } } }
  /experiments/{id}:
    get:  { summary: Experiment detail with stimuli and publish readiness, responses: { "200": { description: OK }, "404": { description: Not found } } }
  /experiments/{id}/stimuli:
    post: { summary: Attach a stimulus (position 1, 2, or 3), responses: { "201": { description: Created }, "422": { description: Validation failed } } }
  /experiments/{id}/publish:
    post: { summary: Publish (requires exactly 3 stimuli), responses: { "200": { description: OK }, "422": { description: Not ready } } }
  /experiments/{id}/sessions:
    post: { summary: Start a participant session (published experiments only), responses: { "201": { description: Created }, "422": { description: Not publishable } } }
  /experiments/{id}/results:
    get:  { summary: Sessions and responses for an experiment, responses: { "200": { description: OK }, "404": { description: Not found } } }
  /sessions/{token}:
    get:  { summary: Session, experiment and stimuli by public token, responses: { "200": { description: OK }, "404": { description: Not found } } }
  /sessions/{token}/consent:
    post: { summary: Accept consent and start the session, responses: { "200": { description: OK } } }
  /sessions/{token}/responses:
    post: { summary: Submit a stimulus response, responses: { "201": { description: Created }, "422": { description: Validation failed } } }
  /sessions/{token}/complete:
    post: { summary: Complete the session, responses: { "200": { description: OK } } }
components:
  schemas:
    KnowledgeObjectSummary:
      type: object
      required: [id,title,type,category,status,version,summary,keywords,tags,path,checksum,relationshipCount]
      properties:
        id: { type: string }
        title: { type: string, maxLength: 200 }
        type: { type: string }
        category: { type: string }
        status: { type: string }
        version: { type: string }
        summary: { type: string, maxLength: 2000 }
        keywords: { type: array, items: { type: string } }
        tags: { type: array, items: { type: string } }
        path: { type: string }
        checksum: { type: string }
        relationshipCount: { type: integer }
    GraphNode:
      type: object
      required: [id,title,type,category]
      properties:
        id: { type: string }
        title: { type: string }
        type: { type: string }
        category: { type: string }
    GraphEdge:
      type: object
      required: [id,source,target,type,resolved]
      properties:
        id: { type: string }
        source: { type: string }
        target: { type: string }
        type: { type: string }
        description: { type: string }
        resolved: { type: boolean }
    ObjectsListResponse:
      type: object
      required: [total,offset,limit,items]
      properties:
        total: { type: integer }
        offset: { type: integer }
        limit: { type: integer }
        items: { type: array, items: { $ref: "#/components/schemas/KnowledgeObjectSummary" } }
    ObjectDetailResponse:
      type: object
      required: [object,relationships]
      properties:
        object: { $ref: "#/components/schemas/KnowledgeObjectSummary" }
        node:   { $ref: "#/components/schemas/GraphNode" }
        relationships:
          type: object
          required: [outgoing,incoming]
          properties:
            outgoing: { type: array, items: { $ref: "#/components/schemas/GraphEdge" } }
            incoming: { type: array, items: { $ref: "#/components/schemas/GraphEdge" } }
    GraphResponse:
      type: object
      required: [generatedAt,schemaVersion,nodeCount,edgeCount,unresolvedEdgeCount,nodes,edges]
      properties:
        generatedAt: { type: string, format: date-time }
        schemaVersion: { type: string }
        nodeCount: { type: integer }
        edgeCount: { type: integer }
        unresolvedEdgeCount: { type: integer }
        nodes: { type: array, items: { $ref: "#/components/schemas/GraphNode" } }
        edges: { type: array, items: { $ref: "#/components/schemas/GraphEdge" } }
    HealthResponse:
      type: object
      required: [status,service,schemaVersion,objects,nodes,edges,generatedAt]
      properties:
        status: { type: string }
        service: { type: string }
        schemaVersion: { type: string }
        objects: { type: integer }
        nodes: { type: integer }
        edges: { type: integer }
        generatedAt: { type: string, format: date-time }
    CreateKnowledgeObjectRequest:
      type: object
      required: [title,type,category,status]
      properties:
        title: { type: string, minLength: 1, maxLength: 200 }
        type: { type: string }
        category: { type: string }
        status: { type: string }
        summary: { type: string, maxLength: 2000 }
        keywords: { type: array, items: { type: string } }
        tags: { type: array, items: { type: string } }
    CreateRelationshipRequest:
      type: object
      required: [sourceObjectId,targetObjectId,type]
      properties:
        sourceObjectId: { type: string, format: uuid }
        targetObjectId: { type: string, format: uuid }
        type: { type: string }
        description: { type: string }
        provenance: { type: string }
        confidence: { type: number, minimum: 0, maximum: 1 }